def is_prime(a):
    for i in range (2,int(a**0.5+1)):
        if a%i==0:
            return 1
    return 0 

a=int(input('请输入一个数字：'))
if is_prime(a):
    print(a,'不是质数')   
else :
    print(a,'是质数')